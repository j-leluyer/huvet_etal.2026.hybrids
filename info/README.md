# info

This directory stores shared ortholog-preprocessing reference artifacts used by scripts in [script/ortho](../script/ortho), including:

- `CA_CG_all.tag.fa`
- `tx2gene.with_mito.tsv`
- `salmon_index/`

These files are generated and consumed by the standalone ortho workflow.
